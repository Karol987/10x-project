import { describe, it, expect } from 'vitest';

/**
 * Example unit test
 * This is a basic example to verify Vitest setup is working
 */
describe('Example Unit Test', () => {
  it('should pass a basic test', () => {
    expect(1 + 1).toBe(2);
  });
});
